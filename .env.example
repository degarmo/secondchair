# Required for the interview, extraction, and answering endpoints.
# The rest of the app (review queue, browsing, seeded data) works without it.
ANTHROPIC_API_KEY=

# Optional overrides
CLAUDE_MODEL=claude-opus-5
DATABASE_URL=postgres://localhost/secondchair
SECRET_KEY=dev-insecure-key-change-in-prod
DEBUG=1
